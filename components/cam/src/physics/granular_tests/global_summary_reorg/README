This is the unit test for the global_summary module.

To execute the test, use the following 4 commands

  ./link_src_files.bash
  
  cd src
  make
  ./exe params.txt

then check either the stdout or the file unitTest_1 for
the message "all results conformed with expected values"
