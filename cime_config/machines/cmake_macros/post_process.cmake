# Error checking?

# Do not edit this file

# These are set to full paths in the main CMakeLists.txt file by the project
# command. We don't want to override the full path every time we load
# the macros in the components.
if (NOT CMAKE_Fortran_COMPILER)
  if (MPILIB STREQUAL "mpi-serial")
    set(CMAKE_Fortran_COMPILER ${SFC})
    set(CMAKE_C_COMPILER ${SCC})
    set(CMAKE_CXX_COMPILER ${SCXX})
  else()
    set(CMAKE_Fortran_COMPILER ${MPIFC})
    set(CMAKE_C_COMPILER ${MPICC})
    set(CMAKE_CXX_COMPILER ${MPICXX})
  endif()
endif()

if (DEBUG)
  string(APPEND CPPDEFS " ${CPPDEFS_DEBUG}")
endif()
