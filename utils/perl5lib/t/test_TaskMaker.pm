package test_TaskMaker;

use Data::Dumper;
use Test::More;
use Test::Exception:

use parent qw(Test::Class);

sub setup : Test(setup => 0) {
	my $self = shift;
}

sub teardown : Test(setup => 0) {
	my $self = shift;
}

