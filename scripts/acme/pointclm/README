# Testing for OIC phase 2
# This will run 10 years of accelerated decomposition spinup (cold start) at the UMBS flux site
# The case directory will be generated in ACME/scripts
# By default, the run directory will be in ACME/run, NOT what is in the machine file.

python pointCLM.py --site US-UMB --ad_spinup --nyears_ad_spinup 10 --compset I1850CLM45CN --compiler <compiler> 

