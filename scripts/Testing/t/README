All test list creation and modification could now concievably be done with manage_xml_entries.
For manage_xml_entries help, please check the script's usage. 

Testing:
This directory contains unit tests for the script ../../manage_testlist
as well as files intended to support the unit tests. 

To run the unit tests, issue the command

   > prove -v test_manage_testlist.t
