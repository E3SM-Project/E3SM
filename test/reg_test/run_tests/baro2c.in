###############################################################
# Restart, and energy conservation test
###############################################################

# The name of this test (should be the basename of this file)
test_name=baro2c
# The type of run (preqx,sweqx,swdgx,etc.)
test_type=preqx
# The specifically compiled executable that this test uses
exec_name=baroC

NUM_CPUS=16

namelist_files=\
  ${HOMME_ROOT}/test/reg_test/${namelist_dir}/${test_name}-run1.nl \
  ${HOMME_ROOT}/test/reg_test/${namelist_dir}/${test_name}-run2.nl
vcoord_files=${HOMME_ROOT}/test/vcoord/*26*

OMP_SUB_TESTS=true
OMP_NUM_THREADS=4
omp_namelist_files=${HOMME_ROOT}/test/reg_test/${namelist_dir}/${test_name}-run2-omp.nl

