###############################################################
# Explicit leapfrog default benchmark
###############################################################
#
# Spectral Element -- swtc5
# NE=30, dt=90, nu=1.5e15, limiter=0, filter_freq=0, NP=4
#
###############################################################

# The name of this test (should be the basename of this file)
test_name=swtc5-mr
# The type of run (preqx,sweqx,swdgx,etc.)
test_type=sweqx
# The specifically compiled executable that this test uses
exec_name=swtcB

NUM_CPUS=16

namelist_files=${HOMME_ROOT}/test/reg_test/namelists/${test_name}.nl
copy_files=${HOMME_ROOT}/test/mesh_refine/grids/mountain_10_x8.g

nc_output_files=swtc51.nc
