#!/bin/bash
#
###############################################################
# Semi-implicit + PIO
###############################################################
#
# Spectral Element -- Polvani et al baroclinic test
# NE=9, dt=600, nu=7e5, filter_freq=0, NP=8, PLEV=20
# (semi-implicit)
#
###############################################################

# The name of this test (should be the basename of this file)
test_name=baro1b
# The type of run (preqx,sweqx,swdgx,etc.)
test_type=preqx
# The specifically compiled executable that this test uses
exec_name=baroB

NCPU=24

namelist_files=${HOMME_ROOT}/test/reg_test/${namelist_dir}/${test_name}.nl
vcoord_files=${HOMME_ROOT}/test/vcoord/*20*
ncl_files=${HOMME_ROOT}/test/reg_test/ncl/${test_name}.ncl

